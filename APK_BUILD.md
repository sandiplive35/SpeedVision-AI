# APK build validation

This branch exists to validate the automated Android APK workflow before distributing the first test build.

Build attempt 3: diagnostics are non-blocking so native compilation can run.
